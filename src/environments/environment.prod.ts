// Environment configuration for production
export const environment = {
  production: true,
  apiUrl: 'https://api.alhilo.com/api/v1',  // Update with your production API URL
  apiTimeout: 30000,
  tokenKey: 'accessToken',
  refreshTokenKey: 'refreshToken',
  userKey: 'currentUser',
};
